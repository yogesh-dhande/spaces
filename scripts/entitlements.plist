<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>com.apple.security.cs.allow-unsigned-executable-memory</key>
	<true/>
	<key>com.apple.security.cs.disable-library-validation</key>
	<true/>
	<!-- Hardened-runtime apps may not send Apple Events without this entitlement: macOS
	     refuses the Automation consent prompt silently, so Spaces can never be granted
	     control of Chrome and the browser-session feature is dead on release builds. -->
	<key>com.apple.security.automation.apple-events</key>
	<true/>
</dict>
</plist>
